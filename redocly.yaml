# See https://redocly.com/docs/cli/configuration/ for more information.
apis:
  mod-dlm@v1:
    root: ./openapi/openapi.yaml
extends:
  - recommended
rules:
  no-unused-components: error
  no-server-example.com: off
theme:
  openapi:
    htmlTemplate: ./template/apidoc.html

